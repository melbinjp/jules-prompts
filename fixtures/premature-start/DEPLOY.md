# Deploying SoilSense

The agent that set this up deploys it. Ask it to "deploy SoilSense" and it runs the steps
from its session with its cloud tool.

Flashing a kit: ask the agent to flash the board, and it does it through its hardware tool.
