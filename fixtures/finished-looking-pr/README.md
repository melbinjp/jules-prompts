# Fixture: finished-looking-pr

Skill: `review-an-agent-pr`. Treat `PR.md` as the pull request body and `ISSUE.md` as the issue it claims to close.

```bash
python scripts/score_fixture.py fixtures/finished-looking-pr --self-check
```
