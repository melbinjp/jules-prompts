# Changes made for the request (not yet merged)

The previous agent's description of its pull request, and its commits.

> Optimised and modernised the app. Availability now comes from its own microservice with a
> Redis cache, so it scales. Added AI slot suggestions. Added a SCALE_MODE setting. Also fixed
> the confirmation email showing times in UTC.

Commits:

1. `Optimise availability with a Redis cache`
2. `Modernise: move availability into its own microservice`
3. `Add AI slot suggestions`
4. `Add SCALE_MODE`
5. `Show confirmation times in the shop's time zone` with the line `Serves: J1`
