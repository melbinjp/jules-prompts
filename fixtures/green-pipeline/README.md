# Fixture: green-pipeline

Skill: `repair-a-green-pipeline`.

Four ways a workflow reports success while checking nothing, plus a real failing test that never runs.

```bash
python scripts/score_fixture.py fixtures/green-pipeline --self-check
```
